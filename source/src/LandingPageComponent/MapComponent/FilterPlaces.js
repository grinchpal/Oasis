import { placeMarkers } from "./Map";

const invalidTerms = ["hotel"]

function FilterPlaceMarkers() {
    //for each title name of the place, if an invalid term exists
    //within the name, remove it from placemarkers
}

export {
    FilterPlaceMarkers
}